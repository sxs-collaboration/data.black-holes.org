FROM php:7-fpm

# RUN groupadd -g 1002 web
# RUN useradd -u 503 -g 1002 -s /bin/bash -G 33 web

# RUN sed -i 's/user = www-data/user = web/; s/group = www-data/group = web/' /usr/local/etc/php-fpm.d/www.conf
